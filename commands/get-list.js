module.exports = {
    name: 'get-list',
    description: 'Obtiene la información de todas las listas o una lista del servidor',
    usage: '<NombreLista?>', 
    args: true,
	execute(message, args) { 
		if (args.length != 1) {
            
		}
        
	},
};